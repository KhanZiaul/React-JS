import React from 'react';
import { useParams } from 'react-router-dom';

const Blog = () => {
    const {title} = useParams()
    return (
        <div>
            
        </div>
    );
};

export default Blog;